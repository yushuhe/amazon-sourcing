weights:
  sales: 0.5
  rating: 0.3
  review_confidence: 0.2

review_confidence_cap: 1000

fallback:
  # When BSR is missing, use review_count * star_rating as sales proxy
  use_review_rating_fallback: true

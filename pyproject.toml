[project]
name = "amazon-sourcing"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.117.1",
]

[tool.vercel]
entrypoint = "api.index:app"

[tool.fastapi]
entrypoint = "api.index:app"
